import React from 'react';
import LocContainer from "./LocContainer";

const TripLocDetail = () => {
    return (
        <div>
            <LocContainer />

        </div>
    );
};

export default TripLocDetail;